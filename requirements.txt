# === Core Framework ===
fastapi==0.115.0
uvicorn[standard]==0.32.0
python-multipart==0.0.18
httpx==0.27.2

# === CrewAI Ecosystem (pinned compatible versions) ===
crewai==0.51.0
crewai-tools==0.8.3

# === LangChain (let crewai pull compatible versions, but pin for safety) ===
langchain==0.2.0
langchain-community==0.2.0
langchain-google-genai==1.0.4

# === Pydantic (must be >=2.8 for crewai 0.51.0) ===
pydantic==2.8.2

# === Vector Database ===
chromadb==0.5.0

# === Embeddings ===
sentence-transformers==2.6.1

# === Observability ===
langsmith==0.1.133

# === Database & Utils ===
supabase==2.3.4
python-dotenv==1.0.1
